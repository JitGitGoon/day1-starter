Starter Repository — Day 1

How to use:
1. Open this folder in VS Code (Remote - WSL).
2. Make the example script executable and run:
   chmod +x rename_ext.sh
   ./rename_ext.sh

3. Initialize git, commit, and (optionally) push to GitHub:
   git init
   git add .
   git commit -m "Day1: starter files"
